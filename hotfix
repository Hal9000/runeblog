#!/usr/bin/env ruby

require 'runeblog'

lib = RuneBlog::Path
puts "path is: #{lib}"

system("cp lib/* #{lib}/")

# Exception in dir structure
cmd = "cp bin/* #{lib.sub(/lib$/, "bin/")}"
puts cmd
system(cmd)
puts
